{
  "name": "ray_charles_engine",
  "version": "1.0.0",
  "description": "A Raycaster",
  "devDependencies": {
    "esbuild": "^0.19.4"
  },
  "scripts": {
    "start": "start firefox http://localhost:8000 && esbuild code.ts --bundle --outdir=dist --watch --serve --servedir=. --banner:js=\"new EventSource(\\\"/esbuild\\\").addEventListener(\\\"change\\\", () => location.reload());\""
  },
  "author": "LordTony",
  "license": "ISC"
}
